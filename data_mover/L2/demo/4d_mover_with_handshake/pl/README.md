The read4D and write4D kernel with ping-pong handshake would generated from API metadata and config, into this folder as `mm2s_4d_hsk.cpp` and `s2mm_4d_hsk.cpp`.
`mm2s_mp` and `s2mm_mp` are used to feed the write4D input, or consume the read4D output.

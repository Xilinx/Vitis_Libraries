# Clarke Direct — ap_fixed precision test

HLS layout matches `clarke_direct_fp32_tb`: `Makefile`, `description.json`, `hls_config*.tmpl`, `run_hls*.tcl`, `src/` (non-template top), `tb/` (golden vs ap_fixed). Run `make run TARGET=csim` from this directory.
